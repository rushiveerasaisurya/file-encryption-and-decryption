# file-encryption-and-decryption
A Flask web app for secure file encryption and decryption using AES‑256 and password‑derived keys. Supports file upload, key generation, encrypted file download, and restoring original files via a simple, responsive HTML/CSS interface.
